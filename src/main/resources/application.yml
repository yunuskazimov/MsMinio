spring:
  datasource:
    password: xazar1
    username: xazar1
    url: jdbc:postgresql://localhost:5400/minio_users
  jpa:
    hibernate:
      ddl-auto: create
    generate-ddl: true
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 5MB
      max-request-size: 10MB
minio:
  url: http://localhost:9000
  bucket: userserver
  access-key: minioo
  secret-key: minio1234
  image-folder: profpic/
  video-folder: video/

file:
  upload:
    acceptableFileExtensions: pdf
    acceptableImageExtensions: jpg,png,jpeg
server:
  port: 8052

client:
  users:
    int:
      url: http://localhost:8051/int/users/id/